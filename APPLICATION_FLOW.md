# 🔄 Financial Management System - Complete Application Flow

This document explains the complete flow of the application from user login to data processing, helping you understand how everything works together.

---

## 🚀 Application Startup Flow

### Backend Startup (`backend/src/server.js`)

```
1. Load environment variables (.env)
   ↓
2. Initialize Express app (app.js)
   - Setup middleware (CORS, Helmet, Body Parser)
   - Configure routes
   - Setup error handling
   ↓
3. Connect to MySQL database (config/db.js)
   - Knex.js connection pool
   ↓
4. Start HTTP server on port 5001
   ↓
5. Initialize Socket.io for real-time communication
   ↓
6. Server ready - listening for requests
```

### Frontend Startup (`frontend/src/main.jsx`)

```
1. React app mounts
   ↓
2. Initialize AuthContext (checks localStorage for token)
   ↓
3. Render App.jsx with routing
   ↓
4. Check authentication status
   ↓
5. Redirect to /login or /dashboard based on auth
   ↓
6. Application ready
```

---

## 🔐 Authentication Flow

### User Login Flow

```
┌─────────────────┐
│  User enters    │
│  email/password │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│ LoginForm.jsx           │
│ - Validates form        │
│ - Calls authService     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ authService.js          │
│ POST /api/auth/login    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ authRoutes.js           │
│ /api/auth/login         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ authController.js       │
│ .login()                │
│ - Validates credentials │
│ - Checks password       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .findUserByEmail()      │
│ - Queries users table   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database          │
│ SELECT * FROM users     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ authController.js       │
│ - Generates JWT token   │
│ - Creates session       │
│ - Returns user + token  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ authService.js          │
│ - Stores token in       │
│   localStorage         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ AuthContext.jsx         │
│ - Updates auth state    │
│ - Sets isAuthenticated  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ App.jsx                │
│ - Redirects to          │
│   /dashboard           │
└─────────────────────────┘
```

### Protected Route Flow

```
User navigates to /customers
         │
         ▼
┌─────────────────────────┐
│ App.jsx                │
│ ProtectedRoute wrapper  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ AuthContext.jsx         │
│ Checks isAuthenticated │
└────────┬────────────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
  Yes        No
    │         │
    │         ▼
    │    ┌─────────────────┐
    │    │ Redirect to    │
    │    │ /login         │
    │    └─────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Render protected page   │
└─────────────────────────┘
```

---

## 👥 Customer Management Flow

### Creating a Customer

```
User fills CustomerForm
         │
         ▼
┌─────────────────────────┐
│ CustomerForm.jsx       │
│ - Form validation      │
│ - onSubmit handler     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ customerService.js     │
│ .createCustomer(data)  │
│ POST /api/customers    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ apiClient.js           │
│ - Adds JWT token to    │
│   Authorization header │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ customerRoutes.js      │
│ POST /api/customers    │
│ - authMiddleware       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ customerController.js  │
│ .createCustomer()      │
│ - Validates input      │
│ - Checks permissions   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .createCustomer()       │
│ - Knex insert query    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ INSERT INTO customers  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns new customer   │
│ with generated ID       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend updates       │
│ customer list          │
└─────────────────────────┘
```

### Viewing Customer List

```
User navigates to /customers
         │
         ▼
┌─────────────────────────┐
│ CustomersList.jsx       │
│ useEffect on mount      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ customerService.js     │
│ .getCustomers()        │
│ GET /api/customers     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ customerController.js  │
│ .getCustomers()        │
│ - Handles pagination    │
│ - Handles search        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .getCustomers()        │
│ - Knex query with      │
│   filters & pagination │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ SELECT * FROM customers │
│ WHERE ... LIMIT ...     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns customer array │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ CustomersList.jsx       │
│ - Renders table        │
│ - Shows pagination     │
└─────────────────────────┘
```

---

## 📄 Invoice Management Flow

### Creating an Invoice

```
User navigates to /invoices/new
         │
         ▼
┌─────────────────────────┐
│ InvoiceForm.jsx         │
│ - Loads customer list   │
│ - Form with items       │
└────────┬────────────────┘
         │
         ▼
User fills form & submits
         │
         ▼
┌─────────────────────────┐
│ invoiceService.js      │
│ .createInvoice(data)   │
│ POST /api/invoices     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ invoiceController.js   │
│ .createInvoice()       │
│ - Validates data        │
│ - Calculates totals     │
│ - Generates invoice #   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .createInvoice()       │
│ - Inserts invoice       │
│ - Stores items as JSON  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ INSERT INTO invoices   │
│ (invoice_number,       │
│  customer_id, total,   │
│  items, status...)     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns invoice        │
│ - Redirects to detail  │
└─────────────────────────┘
```

### Invoice Status Updates

```
Invoice created with status='draft'
         │
         ▼
User clicks "Send Invoice"
         │
         ▼
┌─────────────────────────┐
│ invoiceService.js      │
│ .updateInvoice(id,     │
│   {status: 'sent'})    │
│ PUT /api/invoices/:id  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ invoiceController.js   │
│ .updateInvoice()       │
│ - Updates status        │
│ - Optionally sends      │
│   email (if configured) │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ UPDATE invoices        │
│ SET status='sent'      │
│ WHERE id=...           │
└─────────────────────────┘
```

---

## 💰 Payment Processing Flow

### Recording a Payment

```
User navigates to /payments
         │
         ▼
┌─────────────────────────┐
│ PaymentsPage.jsx        │
│ - Shows payment form     │
│ - Lists invoices        │
└────────┬────────────────┘
         │
         ▼
User selects invoice & enters payment
         │
         ▼
┌─────────────────────────┐
│ paymentService.js      │
│ .createPayment(data)   │
│ POST /api/payments     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ paymentController.js   │
│ .createPayment()        │
│ - Validates amount      │
│ - Checks invoice exists │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .createPayment()       │
│ - Inserts payment       │
│ - Updates invoice       │
│   paid_amount          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ BEGIN TRANSACTION      │
│ INSERT INTO payments   │
│ UPDATE invoices SET    │
│   paid_amount = ...    │
│ COMMIT                 │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Check if invoice is    │
│ fully paid             │
│ - If paid_amount >=    │
│   total_amount:        │
│   status = 'paid'      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns payment        │
│ - Updates UI           │
│ - Shows success toast  │
└─────────────────────────┘
```

---

## 📊 Dashboard Flow

### Real-Time Dashboard Updates

```
User opens /dashboard
         │
         ▼
┌─────────────────────────┐
│ Dashboard.jsx          │
│ - Mounts component      │
│ - Sets up real-time     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ useRealtimeDashboard() │
│ - Connects to Socket.io │
│ - Subscribes to events  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ dashboardService.js    │
│ .getDashboardData()    │
│ GET /api/dashboard     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ dashboardController.js │
│ .getDashboard()         │
│ - Aggregates data from  │
│   multiple tables       │
│ - Calculates KPIs       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ - Queries invoices      │
│ - Queries payments      │
│ - Queries customers     │
│ - Calculates totals     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns dashboard data │
│ - Total receivables     │
│ - Overdue amount        │
│ - Recent invoices       │
│ - Payment trends        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Dashboard.jsx          │
│ - Renders KPIs          │
│ - Renders charts        │
│ - Listens for updates   │
└─────────────────────────┘

When data changes (new payment/invoice):
         │
         ▼
┌─────────────────────────┐
│ Backend emits event     │
│ via Socket.io           │
│ socketService.js        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend receives      │
│ event via Socket.io     │
│ client                  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Dashboard auto-refreshes│
│ - Updates KPIs          │
│ - Updates charts        │
└─────────────────────────┘
```

---

## 📥 Excel Import Flow

### Importing Sales Invoices

```
User navigates to /excel
         │
         ▼
┌─────────────────────────┐
│ ExcelPage.jsx          │
│ - Shows upload button   │
└────────┬────────────────┘
         │
         ▼
User selects Excel file
         │
         ▼
┌─────────────────────────┐
│ ImportModal.jsx         │
│ - File validation       │
│ - Shows preview         │
└────────┬────────────────┘
         │
         ▼
User confirms import
         │
         ▼
┌─────────────────────────┐
│ importService.js       │
│ .importSalesInvoices() │
│ POST /api/import/       │
│   sales-invoices       │
│ - FormData with file    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ importController.js    │
│ .importSalesInvoices() │
│ - Receives file via     │
│   multer middleware     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ excelService.js        │
│ .parseExcelFile()       │
│ - Reads Excel file      │
│ - Validates format      │
│ - Extracts data         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ salesInvoiceImport     │
│ Controller              │
│ - Validates each row    │
│ - Maps to database      │
│   schema                │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .bulkInsertSalesInvoices│
│ - Batch insert          │
│ - Transaction wrapped   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ INSERT INTO            │
│ sales_invoice_master   │
│ (multiple rows)        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns import result  │
│ - Success count         │
│ - Error count           │
│ - Error details         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend shows result  │
│ - Success message       │
│ - Error summary         │
└─────────────────────────┘
```

---

## 🔔 Notification Flow

### System Alert Generation

```
Backend event occurs
(e.g., invoice overdue)
         │
         ▼
┌─────────────────────────┐
│ notificationController │
│ .createAlert()          │
│ - Determines alert type │
│ - Creates message       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .createAlert()          │
│ INSERT INTO alerts      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ socketService.js       │
│ - Emits 'new-alert'     │
│   event via Socket.io   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend receives      │
│ event                  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ AlertsPage.jsx         │
│ - Updates alert list    │
│ - Shows notification    │
│   badge                │
└─────────────────────────┘
```

---

## 📧 Email Notification Flow

### Sending Invoice Email

```
User clicks "Send Invoice Email"
         │
         ▼
┌─────────────────────────┐
│ invoiceService.js      │
│ .sendInvoiceEmail(id)  │
│ POST /api/invoices/:id │
│   /send-email           │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ invoiceController.js   │
│ .sendInvoiceEmail()     │
│ - Gets invoice data     │
│ - Gets customer email   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ pdfService.js          │
│ .generateInvoicePDF()  │
│ - Creates PDF           │
│ - Returns buffer        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ emailService.js        │
│ .sendEmail()            │
│ - Configures Nodemailer │
│ - Attaches PDF          │
│ - Sends via SMTP        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ SMTP Server (Gmail)    │
│ - Delivers email        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns success        │
│ - Updates UI            │
└─────────────────────────┘
```

---

## 🔍 Search & Filter Flow

### Filtering Invoices

```
User applies filters
(status, date range, customer)
         │
         ▼
┌─────────────────────────┐
│ InvoicesList.jsx        │
│ - Updates filter state   │
│ - Triggers useEffect     │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ invoiceService.js      │
│ .getInvoices(filters)  │
│ GET /api/invoices?     │
│   status=sent&          │
│   customer_id=5&        │
│   start_date=...        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ invoiceController.js   │
│ .getInvoices()          │
│ - Parses query params   │
│ - Builds filter object   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .getInvoices(filters)  │
│ - Builds Knex query    │
│   with WHERE clauses    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ SELECT * FROM invoices │
│ WHERE status='sent'    │
│ AND customer_id=5      │
│ AND issue_date >= ...  │
│ ORDER BY ...           │
│ LIMIT ... OFFSET ...   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns filtered list  │
│ - Updates table        │
└─────────────────────────┘
```

---

## 🔄 Real-Time Updates Flow

### Socket.io Connection

```
Frontend app loads
         │
         ▼
┌─────────────────────────┐
│ socketService.js        │
│ (frontend)              │
│ - Connects to           │
│   ws://localhost:5001   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Backend socketService  │
│ - Accepts connection    │
│ - Stores socket in      │
│   connected clients     │
└────────┬────────────────┘
         │
         ▼
When data changes:
         │
         ▼
┌─────────────────────────┐
│ Backend emits event    │
│ io.emit('invoice-created',│
│   invoiceData)          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend receives      │
│ socket.on('invoice-     │
│   created', (data) => { │
│   // Update UI          │
│ })                      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ UI updates             │
│ - Refreshes list        │
│ - Updates dashboard     │
│ - Shows notification    │
└─────────────────────────┘
```

---

## 📈 Report Generation Flow

### Generating Financial Report

```
User navigates to /reports
         │
         ▼
┌─────────────────────────┐
│ ReportsPage.jsx         │
│ - Shows report options  │
│ - Date range picker     │
└────────┬────────────────┘
         │
         ▼
User selects report type & dates
         │
         ▼
┌─────────────────────────┐
│ reportService.js       │
│ .generateReport(type,   │
│   startDate, endDate)  │
│ GET /api/reports?      │
│   type=aging&...        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ reportController.js    │
│ .generateReport()       │
│ - Validates params      │
│ - Determines report type│
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ - Complex queries        │
│ - Aggregations           │
│ - Grouping               │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ MySQL Database         │
│ - Multiple JOINs        │
│ - SUM, COUNT, AVG       │
│ - GROUP BY              │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns report data    │
│ - Formatted for display │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ ReportsPage.jsx         │
│ - Renders report        │
│ - Shows export buttons  │
└─────────────────────────┘

User clicks "Export to PDF"
         │
         ▼
┌─────────────────────────┐
│ pdfService.js          │
│ .generateReportPDF()   │
│ - Creates PDF           │
│ - Returns blob          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Downloads PDF file     │
└─────────────────────────┘
```

---

## 🔐 Session Management Flow

### Multiple Session Tracking

```
User logs in
         │
         ▼
┌─────────────────────────┐
│ authController.js      │
│ .login()                │
│ - Creates JWT token     │
│ - Creates session record│
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .createSession()        │
│ INSERT INTO             │
│ user_sessions           │
│ (session_id, user_id,   │
│  token_hash, ip, ...)   │
└────────┬────────────────┘
         │
         ▼
User logs in from another device
         │
         ▼
┌─────────────────────────┐
│ Another session created │
│ - Both sessions active   │
└────────┬────────────────┘
         │
         ▼
User views active sessions
         │
         ▼
┌─────────────────────────┐
│ sessionService.js      │
│ .getUserSessions()     │
│ GET /api/sessions      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Returns all sessions   │
│ - Current device        │
│ - Other devices         │
│ - Last activity         │
└────────┬────────────────┘
         │
         ▼
User logs out from one device
         │
         ▼
┌─────────────────────────┐
│ sessionService.js      │
│ .logoutSession(id)     │
│ DELETE /api/sessions/:id│
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ repositories.js         │
│ .deleteSession()        │
│ DELETE FROM             │
│ user_sessions           │
│ WHERE session_id=...    │
└─────────────────────────┘
```

---

## 🎯 Key Takeaways

1. **Request Flow**: Frontend → Service → Route → Controller → Repository → Database
2. **Authentication**: JWT tokens stored in localStorage, verified on every protected route
3. **Real-Time**: Socket.io enables live updates without page refresh
4. **Data Validation**: Happens at multiple layers (frontend form, backend controller, database constraints)
5. **Error Handling**: Centralized error handler catches and formats all errors
6. **Transactions**: Critical operations (like payments) use database transactions
7. **File Processing**: Excel/PDF operations happen server-side for security

---

**Last Updated**: January 2025

